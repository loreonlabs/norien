import Chronicle from "@/components/story/Chronicle";

/**
 * THE CHRONICLES OF NORIEN — story.norien.live
 *
 * A cinematic, long-form lore archive (NOT docs, NOT gameplay). The interactive
 * archive lives in the <Chronicle> client island; this route stays a thin shell
 * so metadata/SEO is owned by app/story/layout.tsx. Names are fictional.
 */
export default function StoryPage() {
  return <Chronicle />;
}
